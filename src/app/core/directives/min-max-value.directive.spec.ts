import { MinMaxValueDirective } from './min-max-value.directive';

describe('MinMaxValueDirective', () => {
  it('should create an instance', () => {
    const directive = new MinMaxValueDirective();
    expect(directive).toBeTruthy();
  });
});
